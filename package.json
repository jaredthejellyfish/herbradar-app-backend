{
  "name": "backend",
  "type": "module",
  "scripts": {
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev",
    "supabase:types": "supabase gen types typescript --schema public,account --project-id jxaettwvhscijtierhfr > ./lib/supabase/db-types.ts"
  },
  "dependencies": {
    "hono": "^4.11.4"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}